defaults:
  - /proto_configs/dataset@_here_

name: ffhq256
num_classes: 1 # no label

# path configuration
# change this if you place a dataset in other directory
data_dir: ./data
dataset_root_dir: ${dataset.data_dir}

# augmentation
random_flip: True